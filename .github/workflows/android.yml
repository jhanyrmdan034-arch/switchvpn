name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # این بخش هوشمند، فایل‌های اجرایی گریدل گم‌شده را خودکار دانلود و بازسازی می‌کند
      - name: Restore Missing Gradle Wrapper
        run: |
          gradle wrapper --gradle-version 8.10.2 --distribution-type all

       Tar Setup Gradle Environment
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Grant Execute Permission
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SwitchVPN-Final-APK
          path: app/build/outputs/apk/debug/app-debug.apk
